[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback — let React Router handle client-side routes (e.g. /projects/:id)
# so deep links and refreshes don't 404 in production.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200